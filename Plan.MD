---
name: نقشه راه امکانات حسابداری شبیه حسابفا
overview: نقشه راه چند فازه برای پیاده‌سازی تدریجی امکانات مشابه لیست به‌روزرسانی‌های حسابفا، با اولویت‌بندی بر اساس وابستگی و بدون پیاده‌سازی فوری.
todos: []
isProject: false
---

# نقشه راه امکانات حسابداری (مشابه حسابفا)

منبع مرجع: [لیست تغییرات و به‌روزرسانی‌های حسابفا](https://hesabfa.com/view/UpdatesList). امکانات زیر به فازهای وابسته به هم تقسیم شده‌اند؛ در هر فاز پیاده‌سازی باید **کامل** برای همان دسته انجام شود.

---

## وضعیت فعلی (انجام‌شده)

- ماژول حسابداری با چهار دسته منو (داشبورد، پروژه، CRM، حسابداری)
- سال مالی، نمودار حساب‌ها (کدینگ ایران)، اسناد دستی، دفتر کل، تراز آزمایشی، ترازنامه، سود و زیان، گردش حساب
- تنظیمات ماژول‌ها و تنظیمات حسابداری (واحد پول، بارگذاری کدینگ پیش‌فرض)
- صفحات React برای داشبورد حسابداری، نمودار، اسناد، دفتر، گزارشات، سال مالی، تنظیمات
- **فاز ۱ (اشخاص و کالا):** دسته‌بندی اشخاص/کالا، اشخاص، کالا، واحدها، لیست قیمت، پیش‌فرض‌های کاربر، صفحات اشخاص/کالا/فاکتورها
- **فاز ۲ (فاکتور):** CRUD فاکتور خرید/فروش/پیش‌فاکتور، ردیف‌های کالا، شماره فاکتور، تأیید فاکتور (draft → confirmed). بعد: سند خودکار، چاپ/PDF، لینک به قرارداد
- **فاز ۳ (رسید و پرداخت):** حساب‌های صندوق/بانک/تنخواه (CRUD)، رسید دریافت و رسید پرداخت و انتقال وجه، پیش‌نویس و ثبت، طرف حساب و تسویه فاکتور (فیلد اختیاری). بعد: سند خودکار از رسید/پرداخت، چاپ و اکسل
- **فاز ۴ (چک):** چک دریافتی و پرداختی (شماره، تاریخ، مبلغ، بانک، طرف، سررسید، وضعیت)، وضعیت‌ها: در صندوق، وصول، برگشتی، خرج‌شده؛ عملیات وصول/برگشت/خرج از لیست. بعد: اعلان سررسید، ثبت سند عملیات چک، نمایش در کارت شخص

---

## فاز ۱ – اشخاص و کالا/خدمات پیشرفته

**هدف:** پایه برای فاکتور و انبار و رسید/پرداخت.

- **اشخاص (طرف‌های حساب):** دسته‌بندی اشخاص، اعتبار مالی، شناسه ملی / کد اقتصادی / شماره ثبت، چند تلفن و موبایل، آدرس، نوع شخص (مشتری/تأمین‌کننده/هر دو)، گروه شخص برای محدودیت دسترسی، عکس/لوگو، یادداشت، پیش‌فرض‌های کاربر (شخص پیش‌فرض فاکتور).
- **کالا و خدمات:** دسته‌بندی درختی، واحد اصلی و واحد فرعی و ضریب تبدیل، چند قیمت فروش (لیست قیمت)، قیمت خرید، بارکد (چند بارکد با `;`)، کد کالا یکتا، کنترل موجودی، نقطه سفارش، مالیات خرید/فروش، عکس، یادداشت، غیرفعال کردن. امکان کپی کالا، ورود/خروج اکسل اشخاص و کالا.
- **لیست قیمت:** تعریف لیست‌های قیمت و اختصاص به شخص (پیش‌فرض برای فاکتور).
- **جدول نرخ ارز (برای فاز چندارزی):** ارزها، نرخ تسعیر، تاریخ اعتبار (در همین فاز یا فاز ۹).

وابستگی: بدون این فاز، فاکتور و انبار و رسید فقط با شناسه عددی کار می‌کنند و گزارش‌ها ناقص می‌مانند.

---

## فاز ۲ – فاکتور خرید و فروش

**هدف:** صدور فاکتور رسمی و پیش‌فاکتور، سند خودکار، و ارتباط با قرارداد/سفارش.

- پیش‌فاکتور و فاکتور فروش/خرید با ردیف‌های کالا/خدمات (قیمت، تعداد، تخفیف، مالیات)، شماره فاکتور (سریال، موقت/دائم)، تاریخ سررسید، فروشنده، پروژه، هزینه حمل، سایر اضافات و کسورات، وضعیت (پیش‌نویس، تأیید، مرجوع)، جداسازی شماره فاکتور موقت و دائم در تنظیمات.
- فاکتور سریع (حداقل فیلد برای صدور سریع).
- ثبت خودکار سند حسابداری از فاکتور (حساب دریافتنی/پرداختنی، فروش/خرید، مالیات، تخفیف) با `reference_type`/`reference_id`.
- لینک فاکتور به قرارداد/سفارش (WooCommerce یا داخلی) برای ثبت خودکار.
- چاپ و خروجی PDF/اکسل فاکتور، قالب چاپ (حداقلی؛ فرم‌ساز در فاز بعد).
- محدودیت ویرایش فاکتور (زمان، سطح دسترسی)، تغییر وضعیت پرداخت/ارسال.

وابستگی: وابسته به فاز ۱ (اشخاص و کالا).

---

## فاز ۳ – رسید و پرداخت (دریافت و پرداخت)

**هدف:** ثبت دریافت/پرداخت نقد و چک و انتقال، تسویه فاکتور، سند خودکار.

- **حساب‌های صندوق/بانک/تنخواه:** تعریف بانک، صندوق، تنخواه‌گردان (با توضیحات، شماره کارت/شبا در صورت نیاز).
- **رسید دریافت و رسید پرداخت:** تاریخ، مبلغ، حساب (بانک/صندوق/تنخواه)، طرف حساب، شرح، انتساب به فاکتور (تسویه فاکتور)، انتساب به پروژه، چند ردیف (چند حساب یا چند شخص در یک رسید)، کارمزد بانکی.
- **انتقال بین بانک/صندوق/تنخواه:** انتقال وجه، نرخ ارز در سیستم چندارزی.
- ثبت خودکار سند حسابداری از رسید/پرداخت با حساب‌های پیش‌فرض (قابل تنظیم در تنظیمات مالی).
- چاپ و اکسل رسید/پرداخت، نمایش مانده شخص در چاپ (تنظیمات چاپ).

وابستگی: وابسته به فاز ۱؛ ترجیحاً بعد از فاز ۲ برای تسویه فاکتور.

---

## فاز ۴ – چک (دریافتی و پرداختی)

**هدف:** ثبت چک، وضعیت (در صندوق، وصول، برگشتی، خرج‌شده)، عملیات چک، اعلان سررسید.

- چک دریافتی و پرداختی (شماره، تاریخ، مبلغ، بانک، طرف، تاریخ سررسید، وضعیت)، عودت چک به وضعیت عادی، خرج چک پرداختی، ثبت سند عملیات چک.
- نمایش چک در کارت حساب شخص و در لیست رسید/پرداخت؛ فیلتر و گزارش چک‌های برگشتی.
- اعلان سررسید چک (و فاکتور) در داشبورد و تنظیمات اعلانات.
- پنل پیامک (اختیاری): ارسال پیامک چک پرداختی یا سررسید (وابسته به فاز ۹).

وابستگی: وابسته به فاز ۳.

---

## فاز ۵ – انبارداری

**هدف:** انبارها، رسید و حواله انبار، انبارگردانی، موجودی، ارتباط با فاکتور.

- تعریف انبارها، انبار پیش‌فرض به ازای کاربر.
- رسید انبار و حواله انبار (با ردیف کالا، تعداد، واحد)، حواله انتقال بین انبار، ثبت از اکسل.
- انبارگردانی (موجودی واقعی، اختلاف، روش ارزیابی)، امکان ثبت از اکسل و بارکد.
- ارتباط فاکتور با انبار: صدور خودکار حواله/رسید انبار برای فاکتور (با انتخاب انبار)، کنترل موجودی در حواله (قابل تنظیم).
- گزارش موجودی کالا (به تفکیک انبار، بازه زمانی، واحد فرعی)، کارت حساب کالا (تراکنش‌ها، رسید/حواله، سریال در صورت وجود).
- سند توازن موجودی (دائمی) برای تطبیق حسابداری با انبار.

وابستگی: وابسته به فاز ۱ و ۲.

---

## فاز ۶ – گزارشات و چاپ پیشرفته

**هدف:** پوشش گزارشات مشابه حسابفا و خروجی/چاپ حرفه‌ای.

- گزارش‌های تکمیلی: دفتر روزنامه (تجمیع روزانه/ماهانه، سطح حساب)، تراز آزمایشی ۲/۴/۶/۸ ستونی، تفصیل تراز، مرور حساب‌ها (تجمیع)، گردش حساب بانک/صندوق/تنخواه، گزارش مالیات (تجمیع، فیلتر)، گزارش فروش به تفکیک فروشنده/فاکتور/کالا، خلاصه فاکتور به تفکیک شخص، سود فاکتور، کارت پروژه، صورتحساب سرمایه.
- فیلتر پیشرفته: پروژه، بازه سند، بدون اسناد افتتاحیه/اختتامیه، سطح حساب.
- تنظیمات چاپ: سایز کاغذ، انتخاب و جابجایی ستون‌ها، حاشیه، نمایش/عدم نمایش تاریخ و سال مالی در گزارش.
- خروجی اکسل و PDF برای تمام گزارش‌های اصلی و لیست‌ها (اسناد، فاکتورها، رسیدها، اشخاص، کالا، چک‌ها، انبار).

وابستگی: وابسته به فازهای ۱–۵ برای داده واقعی.

---

## فاز ۷ – سامانه مودیان و امور مالیاتی

**هدف:** تطبیق با الزامات مالیاتی ایران و دفاتر قانونی.

- تنظیمات سامانه مودیان (احراز هویت، کلیدها)، ارسال صورتحساب به مودیان (با تأیید کاربر)، دریافت شماره منحصر به فرد مالیاتی و وضعیت ارسال، ارسال فاکتور ابطالی، الگوی پیمانکاری در صورت نیاز.
- ساخت فایل TTMS (صورت معاملات فصلی) برای اداره دارایی، خروجی مناسب برای دفاتر الکترونیکی (طبق الزامات جاری).
- فیلدهای لازم در فاکتور/شخص: شناسه ملی، کد اقتصادی، کد مالیاتی کالا، سریال فاکتور، شماره منحصر به فرد مالیاتی.
- گزارش مالیات و فیلترهای مرتبط با ارسال به مودیان.

وابستگی: وابسته به فاز ۲ و ۱؛ ترجیحاً بعد از پایدار شدن فاکتور و اشخاص.

---

## فاز ۸ – پیشرفته: پروژه، کاربران، داشبورد، حقوق

**هدف:** چندپروژه، سطوح دسترسی ریز، داشبورد سفارشی، حقوق و دستمزد.

- **پروژه:** انتساب سند/فاکتور/رسید به پروژه، محدودیت دسترسی کاربر به پروژه، گزارش کارت پروژه و فیلتر بر اساس پروژه در گزارشات.
- **مدیریت کاربران:** سطوح دسترسی تفکیک‌شده (مثلاً تأیید سند، تأیید فاکتور، ویرایش فاکتور، نمایش قیمت فاکتور، دسترسی به انبار/بانک/تنخواه، محدودیت به اسناد خود کاربر، محدودیت به گروه شخص)، مهلت ویرایش اسناد/فاکتور، محدودیت روز و ساعت دسترسی.
- **داشبورد:** ابزارک‌های قابل تنظیم (بدهکاران/بستانکاران، موجودی کالا، تقویم، نرخ ارز، یادداشت، جستجو در اسناد/فاکتور/رسید)، ذخیره بازه زمانی و تنظیمات ابزارها.
- **حقوق و دستمزد:** سند حقوق (حداقلی)، کسورات و اضافات، فیش حقوقی در چاپ/اکسل، ارتباط با حساب‌های تعریف‌شده (در صورت نیاز).
- **فروش اقساطی (اختیاری):** قرارداد اقساط، اعلان سررسید اقساط.

وابستگی: پروژه و کاربران وابسته به فازهای قبل؛ حقوق و اقساط اختیاری.

---

## فاز ۹ – یکپارچه‌سازی و چندارزی و ارتباطات

**هدف:** API، چندارزی، ارتباط با دنیای بیرون و چاپ/فرم حرفه‌ای.

- **API:** گسترش REST/API برای اشخاص، کالا، فاکتور، رسید/پرداخت، انبار، گزارشات (مشابه مستندات حسابفا)، احراز هویت (توکن/کلید)، جلوگیری از پردازش درخواست تکراری (idempotency).
- **چندارزی:** ارز اصلی و ارزهای دیگر، نرخ تسعیر (جدول تبدیل)، ذخیره نرخ در سند/تراز افتتاحیه، گزارش‌های ارزی و مانده/گردش ارزی در کارت حساب شخص.
- **پنل پیامک و اعلان:** ارسال دستی/اتوماتیک (سررسید فاکتور، چک، مانده بدهی)، قالب پیامک فاکتور، اعلانات در سیستم و در صورت وجود اپ موبایل.
- **فرم‌ساز (قالب چاپ):** طراحی قالب چاپ فاکتور، رسید، حواله، سند، چک، لیبل بارکد (اندازه و فیلدها)، با فیلدهای داینامیک (مشابه لیست تغییرات حسابفا).
- **بارکد و سریال:** چاپ بارکد کالا (تعدادی/وزنی)، خواندن بارکد در فاکتور و انبار، ثبت شماره سریال کالا در فاکتور و انبارگردانی.

وابستگی: API و چندارزی مستقل؛ فرم‌ساز و بارکد وابسته به فازهای ۲، ۳، ۵.

---

## فاز ۱۰ – رفع نقص و بهینه‌سازی (مستمر)

- راس‌گیری فاکتور و چک، کپی سند/فاکتور/رسید، قفل سند، تاریخچه ویرایش اسناد.
- جستجوی پیشرفته در جداول (تاریخ، دسته‌بندی، متن)، انتخاب پیشرفته ردیف‌ها، چاپ ردیف‌های انتخاب‌شده.
- هشدارها: قیمت فروش کمتر از خرید، کد/بارکد/نام تکراری، شماره چک تکراری، موجودی منفی در حواله (در صورت فعال بودن کنترل).
- بهینه‌سازی عملکرد لیست‌ها و گزارش‌ها، کش و صفحه‌بندی مناسب.

---

## خلاصه و ترتیب پیشنهادی


| فاز       | محتوا                                               | وابستگی    |
| --------- | --------------------------------------------------- | ---------- |
| انجام‌شده | سال مالی، کدینگ، اسناد، دفتر، گزارشات پایه، تنظیمات | —          |
| ۱         | اشخاص و کالا/خدمات پیشرفته، لیست قیمت               | —          |
| ۲         | فاکتور خرید/فروش، سند خودکار، چاپ                   | فاز ۱      |
| ۳         | رسید و پرداخت، بانک/صندوق/تنخواه                    | فاز ۱      |
| ۴         | چک دریافتی/پرداختی، اعلان سررسید                    | فاز ۳      |
| ۵         | انبارداری (رسید/حواله/انبارگردانی، موجودی)          | فاز ۱، ۲   |
| ۶         | گزارشات و چاپ پیشرفته، اکسل/PDF                     | فازهای ۱–۵ |
| ۷         | سامانه مودیان، TTMS، دفاتر الکترونیکی               | فاز ۱، ۲   |
| ۸         | پروژه، کاربران، داشبورد، حقوق/اقساط                 | فازهای قبل |
| ۹         | API، چندارزی، پیامک، فرم‌ساز، بارکد/سریال           | فازهای قبل |
| ۱۰        | رفع نقص و بهینه‌سازی                                | مستمر      |


این نقشه راه به‌صورت فازبه‌فاز قابل اجرا است؛ در هر فاز می‌توان فقط همان دسته را «کامل» پیاده کرد و در فاز بعد به سراغ دسته بعدی رفت. برای شروع اجرا، پیشنهاد می‌شود با **فاز ۱ (اشخاص و کالا پیشرفته)** آغاز شود.